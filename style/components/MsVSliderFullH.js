document.addEventListener("DOMContentLoaded",function(){var SLIDE_INTERVAL=1e4;var BORDER_COLOR="#ffffff";var BORDER_WIDTH=2;var sliderContainer=document.querySelector(".slider-container");var slides=sliderContainer.querySelectorAll(".slide");var bullets=document.querySelectorAll(".bullet");var currentSlideIndex=0;var slideInterval;function nextSlide(){if(window.innerWidth<768)return;currentSlideIndex=(currentSlideIndex+1)%slides.length;var slideWidth=slides[0].offsetWidth;sliderContainer.scrollTo({left:slideWidth*currentSlideIndex,behavior:"smooth"});updateBullets(currentSlideIndex)}function startSlideTimer(){if(window.innerWidth>=768){stopSlideTimer();updateBullets(currentSlideIndex);slideInterval=setInterval(nextSlide,SLIDE_INTERVAL)}}function stopSlideTimer(){if(slideInterval){clearInterval(slideInterval);slideInterval=null}}bullets.forEach(function(bullet,index){bullet.addEventListener("click",function(){currentSlideIndex=index;var slideWidth=slides[0].offsetWidth;sliderContainer.scrollTo({left:slideWidth*index,behavior:"smooth"});updateBullets(index);stopSlideTimer();startSlideTimer()})});function drawCircularBorder(canvas,progress){var ctx=canvas.getContext("2d");var width=canvas.width;var height=canvas.height;var centerX=width/2;var centerY=height/2;var radius=width/2-BORDER_WIDTH;ctx.clearRect(0,0,width,height);ctx.strokeStyle=BORDER_COLOR;ctx.lineWidth=BORDER_WIDTH;ctx.beginPath();ctx.arc(centerX,centerY,radius,-Math.PI/2,-Math.PI/2+2*Math.PI*progress);ctx.stroke()}function updateBullets(currentIndex){bullets.forEach(function(bullet){var existingCanvas=bullet.querySelector("canvas");if(existingCanvas){existingCanvas.remove()}bullet.classList.remove("active")});var currentBullet=bullets[currentIndex];if(currentBullet){currentBullet.classList.add("active");var newCanvas=document.createElement("canvas");newCanvas.width=60;newCanvas.height=60;currentBullet.querySelector(".bullet-border").appendChild(newCanvas);var startTime=null;function animate(currentTime){if(!startTime)startTime=currentTime;var progress=(currentTime-startTime)/SLIDE_INTERVAL;if(progress<=1){drawCircularBorder(newCanvas,progress);requestAnimationFrame(animate)}}requestAnimationFrame(animate)}}currentSlideIndex=0;updateBullets(currentSlideIndex);startSlideTimer();var lastSlide=slides[slides.length-1];var additionalContent=document.querySelector(".additional-content");var isScrolling=false;function checkLastSlideAndAdditionalContent(){var lastSlideRect=lastSlide.getBoundingClientRect();var additionalContentRect=additionalContent.getBoundingClientRect();if(window.innerWidth>=768){if(lastSlideRect.left<=window.innerWidth&&additionalContentRect.left<window.innerWidth){sliderContainer.classList.add("no-slider")}else if(slides[0].getBoundingClientRect().left>0){sliderContainer.classList.remove("no-slider")}}else{if(lastSlideRect.top<=0&&additionalContentRect.top<window.innerHeight){sliderContainer.classList.add("no-slider")}else if(slides[0].getBoundingClientRect().top>0){sliderContainer.classList.remove("no-slider")}}}function snapToNearestSlide(){if(isScrolling)return;var viewportWidth=window.innerWidth;var viewportHeight=window.innerHeight;var closestSlide=null;var closestDistance=Infinity;slides.forEach(function(slide,index){var rect=slide.getBoundingClientRect();var distance=window.innerWidth>=768?Math.abs(rect.left):Math.abs(rect.top);var threshold=window.innerWidth>=768?viewportWidth*.3:viewportHeight*.3;if(distance<closestDistance&&distance<threshold){closestSlide=slide;closestDistance=distance;currentSlideIndex=index}});if(closestSlide){isScrolling=true;if(window.innerWidth>=768){var targetScroll=window.scrollX+closestSlide.getBoundingClientRect().left;window.scrollTo({left:targetScroll,behavior:"smooth"});updateBullets(currentSlideIndex)}else{var targetScroll1=window.scrollY+closestSlide.getBoundingClientRect().top;window.scrollTo({top:targetScroll1,behavior:"smooth"})}setTimeout(function(){isScrolling=false},1e3)}}function checkCurrentSlideOnScroll(){if(window.innerWidth<768)return;var scrollLeft=sliderContainer.scrollLeft;var slideWidth=slides[0].offsetWidth;var newIndex=Math.round(scrollLeft/slideWidth);if(newIndex!==currentSlideIndex){currentSlideIndex=newIndex;updateBullets(newIndex)}}var scrollTimeout;window.addEventListener("scroll",function(){if(window.innerWidth<768){clearTimeout(scrollTimeout);scrollTimeout=setTimeout(function(){snapToNearestSlide()},100)}});sliderContainer.addEventListener("scroll",function(){checkLastSlideAndAdditionalContent();checkCurrentSlideOnScroll();if(window.innerWidth>=768){clearTimeout(scrollTimeout);scrollTimeout=setTimeout(function(){snapToNearestSlide();stopSlideTimer();startSlideTimer()},100)}});sliderContainer.addEventListener("mouseenter",stopSlideTimer);sliderContainer.addEventListener("touchstart",stopSlideTimer);sliderContainer.addEventListener("mouseleave",startSlideTimer);sliderContainer.addEventListener("touchend",function(){setTimeout(startSlideTimer,1e3)});window.addEventListener("resize",function(){if(window.innerWidth>=768){startSlideTimer()}else{stopSlideTimer()}});checkLastSlideAndAdditionalContent()});function ajustarTamanoVideoContainer(){var videoContainers=document.querySelectorAll(".video-container");var jwplayer=document.querySelector(".jwplayer");var alturaViewport=window.innerHeight;if(jwplayer){var anchoOriginal=jwplayer.offsetWidth;var altoOriginal=jwplayer.offsetHeight;var relacionAspecto=anchoOriginal/altoOriginal;var nuevoAncho=Math.round(alturaViewport*relacionAspecto);videoContainers.forEach(function(videoContainer){videoContainer.style.width="".concat(Math.round(nuevoAncho),"px");videoContainer.style.height="".concat(Math.round(alturaViewport),"px")})}}window.addEventListener("load",ajustarTamanoVideoContainer);window.addEventListener("resize",ajustarTamanoVideoContainer);